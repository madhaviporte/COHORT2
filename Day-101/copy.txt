Authentication
.)register[1) user ka data save krna 2) token user ko dena]
.)login
.)logout(token blacklisting)
.)[OTP based registratiion]

Post
.) create
.) can see the feed
.) like posts (collection types)
.) save post

users
.) followers
.) following


user{
    username: String,
    email:String,
    password:String,
    bio:String,
    followers:Array,
    profile_image:String
}

Q)why we dont send password in response when we register a user in bolits points